# Next.js Docker Deployment

Proyek ini mendemonstrasikan cara melakukan containerization pada aplikasi Next.js menggunakan Docker dengan teknik **Multi-stage Build** untuk efisiensi ukuran image.

## Prasyarat
- Docker terinstall
- Docker Compose terinstall

## Cara Menjalankan

1. **Build dan Run menggunakan Docker Compose:**
   ```bash
   docker-compose up --build -d
